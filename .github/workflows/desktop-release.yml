name: Desktop Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  validate-release:
    name: Validate release tag
    runs-on: ubuntu-24.04
    outputs:
      release_tag: ${{ steps.validate.outputs.release_tag }}
      release_version: ${{ steps.validate.outputs.release_version }}
      prerelease: ${{ steps.validate.outputs.prerelease }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Validate tag format and main ancestry
        id: validate
        shell: bash
        run: |
          set -euo pipefail

          tag="${GITHUB_REF_NAME}"

          if [[ ! "$tag" =~ ^v([0-9]+\.[0-9]+\.[0-9]+(-[0-9A-Za-z.-]+)?(\+[0-9A-Za-z.-]+)?)$ ]]; then
            echo "Release tags must match v<major>.<minor>.<patch>[-prerelease][+build]. Received: $tag" >&2
            exit 1
          fi

          version="${BASH_REMATCH[1]}"

          git fetch origin main --depth=1

          if ! git merge-base --is-ancestor "$GITHUB_SHA" origin/main; then
            echo "Tag $tag points to commit $GITHUB_SHA, which is not contained in origin/main." >&2
            exit 1
          fi

          prerelease=false
          if [[ "$version" == *-* ]]; then
            prerelease=true
          fi

          echo "release_tag=$tag" >> "$GITHUB_OUTPUT"
          echo "release_version=$version" >> "$GITHUB_OUTPUT"
          echo "prerelease=$prerelease" >> "$GITHUB_OUTPUT"

  build-desktop:
    name: Build ${{ matrix.platform }} release
    needs: validate-release
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: linux
            runner: ubuntu-24.04
            target: x86_64-unknown-linux-gnu
            architecture: x64
            bundles: deb,appimage,rpm
            prerelease_bundles: deb,appimage,rpm
            artifact_paths: |
              src-tauri/target/${{ matrix.target }}/release/bundle/deb/*.deb
              src-tauri/target/${{ matrix.target }}/release/bundle/appimage/*.AppImage
              src-tauri/target/${{ matrix.target }}/release/bundle/rpm/*.rpm
          - platform: windows
            runner: windows-latest
            target: x86_64-pc-windows-msvc
            architecture: x64
            bundles: nsis,msi
            prerelease_bundles: nsis
            artifact_paths: |
              src-tauri/target/${{ matrix.target }}/release/bundle/nsis/*.exe
              src-tauri/target/${{ matrix.target }}/release/bundle/msi/*.msi
          # Temporarily disabled: GitHub-hosted macOS runners have been sitting queued too long.
          # Re-enable once runner availability is acceptable again.
          # - platform: macos
          #   runner: macos-13
          #   target: x86_64-apple-darwin
          #   architecture: x64
          #   bundles: dmg
          #   prerelease_bundles: dmg
          #   artifact_paths: |
          #     src-tauri/target/${{ matrix.target }}/release/bundle/dmg/*.dmg
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install Linux build dependencies
        if: matrix.platform == 'linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libgtk-3-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            patchelf

      - name: Install frontend dependencies
        run: npm ci

      - name: Inject release version
        run: node scripts/prepare-release-version.mjs "${{ needs.validate-release.outputs.release_version }}"

      - name: Build desktop bundles
        shell: bash
        env:
          BUNDLES: ${{ matrix.bundles }}
          PRERELEASE_BUNDLES: ${{ matrix.prerelease_bundles }}
          PRERELEASE: ${{ needs.validate-release.outputs.prerelease }}
          TARGET: ${{ matrix.target }}
        run: |
          set -euo pipefail

          selected_bundles="$BUNDLES"
          if [[ "$PRERELEASE" == "true" && -n "${PRERELEASE_BUNDLES:-}" ]]; then
            selected_bundles="$PRERELEASE_BUNDLES"
          fi

          args=(build --ci --target "$TARGET")
          if [[ -n "$selected_bundles" ]]; then
            args+=(--bundles "$selected_bundles")
          fi

          npm run tauri -- "${args[@]}"

      - name: Verify bundle output
        shell: bash
        run: |
          set -euo pipefail
          find src-tauri/target -path '*/release/bundle/*' -type f | sort

      - name: Upload bundled artifacts
        uses: actions/upload-artifact@v4
        with:
          name: archimedes-agent-${{ matrix.platform }}-${{ matrix.architecture }}-v${{ needs.validate-release.outputs.release_version }}
          if-no-files-found: error
          path: ${{ matrix.artifact_paths }}

  publish-release:
    name: Publish GitHub release
    if: ${{ always() && needs.validate-release.result == 'success' }}
    needs:
      - validate-release
      - build-desktop
    runs-on: ubuntu-24.04
    steps:
      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          path: release-artifacts
          pattern: archimedes-agent-*
          merge-multiple: false

      - name: Publish GitHub release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ needs.validate-release.outputs.release_tag }}
          name: Archimedes Agent ${{ needs.validate-release.outputs.release_tag }}
          generate_release_notes: true
          prerelease: ${{ needs.validate-release.outputs.prerelease == 'true' }}
          fail_on_unmatched_files: true
          files: |
            release-artifacts/**/*
