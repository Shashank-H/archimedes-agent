export const DEFAULT_SIDEBAR_WIDTH = 420;
export const MIN_SIDEBAR_WIDTH = 320;
export const MAX_SIDEBAR_WIDTH = 720;
export const MIN_CANVAS_WIDTH = 420;

export const WORKSPACE_WINDOW_CONTROL_LABELS = {
  minimize: 'Minimize window',
  maximize: 'Maximize window',
  restore: 'Restore window',
  close: 'Close window',
} as const;

export const WORKSPACE_NATIVE_CONTROL_PLATFORMS = ['windows', 'linux'] as const;
